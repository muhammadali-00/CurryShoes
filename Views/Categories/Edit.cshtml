@model Lab_Asssignment._1.Models.Category

@{
    ViewBag.Title = "Edit";
    Layout = "~/Views/Website/Admin.cshtml";
}

<h4>Edit Category</h4>

@using (Html.BeginForm())
{
    @Html.AntiForgeryToken()
    
    <div class="form-horizontal">
        <hr />
        @Html.ValidationSummary(true, "", new { @class = "text-danger" })
        @Html.HiddenFor(model => model.ctgry_ID)

        <div class="form-group">
            @Html.LabelFor(model => model.ctgry_name, htmlAttributes: new { @class = "control-label col-md-2" })
            <div class="col-md-10 mt-3">
                @Html.EditorFor(model => model.ctgry_name, new { htmlAttributes = new { @class = "form-control" } })
                @Html.ValidationMessageFor(model => model.ctgry_name, "", new { @class = "text-danger" })
            </div>
        </div>

        <div class="form-group my-3">
            <div class="col-md-offset-2 col-md-10">
                <input type="submit" value="Update" class="btn btn-default btn-danger" />
            </div>
        </div>
    </div>
}

<div>
    @Html.ActionLink("Back to List", "Index")
</div>

@model IEnumerable<Lab_Asssignment._1.Models.Category>

@{
    ViewBag.Title = "Index";
    Layout = "~/Views/Website/Admin.cshtml";
}

<h4>Categories</h4>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.ctgry_ID)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.ctgry_name)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.ctgry_ID)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.ctgry_name)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id = item.ctgry_ID }) |
            @Html.ActionLink("Details", "Details", new { id = item.ctgry_ID }) |
            @Html.ActionLink("Delete", "Delete", new { id = item.ctgry_ID })
        </td>
    </tr>
}

</table>
